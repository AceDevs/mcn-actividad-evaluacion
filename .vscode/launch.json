{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/actividad-evaluacion/package.json",
                "localRoot": "${workspaceFolder}/actividad-evaluacion"
            }
        }
    ]
}